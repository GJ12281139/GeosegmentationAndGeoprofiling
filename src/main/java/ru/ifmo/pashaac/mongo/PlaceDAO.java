package ru.ifmo.pashaac.mongo;

/**
 * Created by Pavel Asadchiy
 * 30.04.16 9:35.
 */
public class PlaceDAO {
//    ApplicationContext ctx = new AnnotationConfigApplicationContext(SpringMongoConfig.class);
//    MongoOperations mongoOperation = (MongoOperations) ctx.getBean("mongoTemplate");
//    String country = boxService.getCountryByGeocodings(mapService.getAddressByCoordinates(model.getUser().getLatLng()));
//    if (!mongoOperation.collectionExists(country)) {
//        mongoOperation.createCollection(country);
//    }
//    String region = boxService.getRegionByGeocodings(mapService.getAddressByCoordinates(model.getUser().getLatLng()));
//    places.stream().filter(place -> place.getPlaceId() != null).forEach(place -> mongoOperation.save(place, region));

}
